info:
  name: create
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/teachers"
  body:
    type: json
    data: |-
      {
        "name": "Said Ali"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
